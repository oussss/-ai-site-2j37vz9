import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
// Note: Tailwind directives are loaded via CDN in index.html for this generated example 
// to ensure immediate visual fidelity without build steps.
// In a real production environment, you would import './index.css' here.

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)